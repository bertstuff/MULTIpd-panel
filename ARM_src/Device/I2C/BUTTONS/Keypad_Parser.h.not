#ifndef __KEYPAD_PARSER_H__
#define __KEYPAD_PARSER_H__
	#include "Buttons_3x4.h"
	#include "Keypad_3x4.h"

	#define C_RETURN 'X'
	#define C_OK 'V'
	#define C_IDLE 0xFF

	bool keypad_pressed(process_event_t ev);

	uint8_t keypad_button(process_event_t ev, process_data_t data);

#endif //__KEYPAD_PARSER_H__
